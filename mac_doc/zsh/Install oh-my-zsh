Install oh-my-zsh 
Install oh-my-zsh now

http://ohmyz.sh/

Via curl Via wget
$ sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

example:
 ~  sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

You already have Oh My Zsh installed.
You'll need to remove /Users/wenwen/.oh-my-zsh if you want to re-install.
➜  ~
➜  ~

==enable plugin====

Enable the plugins you want by editing your ~/.zshrc file.

Example:

plugins=(rails git ruby)
plugins=(git autojump osx)

osx 插件

man-preview 通过 preview 程序查看一个命令的手册
quick-look 快速预览文件
pfd 返回当前 Finder 打开的文件夹的路径
cdf 切换到当前 Finder 所在的目录